import React from "react";

// Placeholder — replaced by the repaired verse-pair reader in a later task.
export default function Reader({ state, navigate }) {
  return <div data-testid="xref-reader" className="xref-reader" />;
}
