import React from "react";

// Placeholder — replaced by the ribbon overview in a later task.
export default function Overview({ navigate }) {
  return <div data-testid="xref-overview" className="xref-overview" />;
}
