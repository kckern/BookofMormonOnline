# Retired legacy scenario.
#
# The previous scenario embedded the archived channel ID, requested membership
# in a now-fixed group, created an outsider root post, and expected the removed
# immediate bot cascade. Use a reviewed environment-local scenario built from
# the external flagship configuration and the validation matrix in:
# docs/audits/2026-08-29-chat-studygroup-reactivation-readiness.md
