# Included by Nginx Proxy Manager in every generated proxy server block. The
# `if` parameter is the safe access_log condition, not an nginx rewrite `if`.
access_log /data/logs/bom-telemetry.log bom_telemetry if=$bom_telemetry_enabled;
